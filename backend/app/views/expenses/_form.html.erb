<%= form_for(@expense) do |f| %>
  <% if @expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% @expense.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
  <tr>
    <td>Склад</td>
	<td width=100></td>
    <td><%= f.collection_select :stock_id, Stock.all.collect, :id, :name, {:include_blank=> true}, :style=>'width:400px;' %></td>
  </tr>
  <tr>
	<td>Период</td>
	<td></td>
	<td>
	<%= select_month @expense.period, :fieldname=>"period_month" %>
	<%= select_year  @expense.period, :fieldname=>"period_year" %>
	</td>
  </tr>	
  <tr>
    <td>Сумма</td>
	<td></td>
	<td><%= f.text_field :value, :size=>30 , :style=>'width:400px;' %></td>
  </tr>
  <tr>
    <td>Описание</td><td></td>
	<td><%= f.text_field :comment, :size=>30 , :style=>'width:400px;' %></td>
  </tr>
  </table>
  <div class="actions">
    <%= f.submit "Сохранить"%>
  </div>
<% end %>
